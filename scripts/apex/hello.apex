// System.debug('@@@CHAN -> ' + MW_OpenWeatherService.UnitType.valueOf('metrics'));
// String tmp = 'metric';
// Set<String> unitTypes = MW_OpenWeatherService.UnitType.values();

// System.debug('@@@CHAN -> ' + unitTypes);

System.debug('@@@CHAN -> ' + MW_OpenWeatherController.getCurrentWeatherForecastService(null, null, null));
